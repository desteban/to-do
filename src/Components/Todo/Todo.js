export class Todo {

    /**
     * Método para inicializar un Todo
     * @param {string} title 
     * @param {boolean} complete 
     */
  constructor(title, complete) {
    this.title = title;
    this.complete = complete;
  }
}
